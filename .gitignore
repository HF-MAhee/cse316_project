*.elf
*.hex
*.o
# Header-dependency files from the Makefile's -MMD -MP. They arrived with
# the per-mode build split and were never added here, so every build left
# a drift of untracked files behind.
*.d
*.aux
*.fls
*.nav
*.out
*.toc
*.snm
*.fdb_latexmk
.DS_Store
.vscode

# Local Claude Code skills tooling (npx skills add ...) -- not part of the
# firmware project, kept out of history intentionally.
.agents/
.claude/
skills-lock.json